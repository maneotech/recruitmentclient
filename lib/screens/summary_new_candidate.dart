import 'package:flutter/material.dart';
import 'package:recruitmentclient/screens/base_screen.dart';

class SummaryNewCandidate extends StatelessWidget {
  const SummaryNewCandidate({super.key});

  @override
  Widget build(BuildContext context) {
    return BaseScreen(
      Text("Fin de l'import (peut servir de page de sommaire)"),
    );
  }
}
